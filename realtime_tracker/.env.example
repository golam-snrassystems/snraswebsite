# Copy this file to .env and fill in real credentials. Do NOT commit the real .env
# Example environment variables for the realtime_tracker backend
DB_HOST=localhost
DB_USER=snras_api
DB_PASSWORD=StrongPasswordHere
DB_NAME=waterQuality

# Optional: override server port
PORT=8345

# MQTT broker (if different from defaults)
MQTT_BROKER=mqtt://34.100.201.70:8883
MQTT_USER=snras_client_un
MQTT_PASS=SnraS_ProJ
